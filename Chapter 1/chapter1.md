# Chapter 1 

In Chapter 1, I solved basic problems and mostly used `print`. I also used an external module `pyttsx3` for problem 3.

To install:
```bash
pip install pyttsx3
```

Problem 2 was supposed to be done using REPL (Read–Eval–Print Loop), but I used a function instead. To use REPL:

* open terminal
* type `python`
* run:
  5 * 1 ... 5 * 10

Problem 4 was solved using ChatGPT. I think you will fall behind if you don’t use available tools at your disposal. With that being said, I’m not going to use LLMs to generate complete solutions until I finish the basics and fundamentals.

> [!NOTE]
> This markdown file was written by me. Grammar and other corrections were fixed later using LLMs.